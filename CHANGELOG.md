# 更新记录

## 4.4.1

- 修复阅读容器初始隐藏、尚无宽高时导航失败的问题：等待容器具有有效尺寸后再布局和恢复阅读位置；关闭阅读器会取消等待。
- 笔记设置支持直接输入尚不存在的保存目录；新建摘录笔记和书籍笔记时可选择或输入目录，后续新建记住该选择。支持仓库根目录和多级目录，不移动已有笔记。
- 旧配置中空书籍笔记目录/空的上次摘录目录原先继承摘录目录，升级时只迁移一次以保留保存位置；之后明确选择根目录不会再回退。
- 修复部分新建书籍笔记入口跳过路径选择，以及明确选择根目录后仍保存到默认目录的问题。目录无法创建时不再静默退回根目录。
- 原生 AI 伴读支持从 Obsidian 文件树拖入整本 EPUB：本地按阅读顺序提取正文，显示书名、字数及可移除的上下文附件，发送前不调用模型。
- 长书明确标注只包含前 180,000 字符。限制单文件 30 MiB、目录声明及累计实际解压 100 MiB、单条目 8 MiB、10,000 个 ZIP 条目和 2,000 个章节，阻止损坏、加密或无正文的文件误作可用上下文。
- 附件提取期间拦截发送；移除、关闭、切换书籍或会话、再次拖入时不会回填过期结果。附件来源随本轮上下文保存。
